{
  "name": "mcp-workspace",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install": "npm run install:sdk && npm run install:server",
    "install:sdk": "cd sdk && npm install && cd ..",
    "install:server": "cd server && npm install"
  }
}